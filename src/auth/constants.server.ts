/** Session cookie and token lifetime defaults for auth (Phase 1+). */
export const SESSION_COOKIE_NAME = "tg_session";
export const SESSION_DURATION_MS = 30 * 24 * 60 * 60 * 1000;
export const MAGIC_LINK_DURATION_MS = 15 * 60 * 1000;
